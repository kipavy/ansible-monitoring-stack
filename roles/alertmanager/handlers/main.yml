# roles/alertmanager/handlers/main.yml - Reload Alertmanager config via HTTP API
---
- name: Reload Alertmanager
  ansible.builtin.uri:
    url: "http://localhost:{{ alertmanager_port }}/-/reload"
    method: POST
    status_code: 200
  delegate_to: localhost

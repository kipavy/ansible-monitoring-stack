# roles/prometheus/handlers/main.yml - Reload Prometheus config via HTTP API
---
- name: Reload Prometheus
  ansible.builtin.uri:
    url: "http://localhost:{{ prometheus_port }}/-/reload"
    method: POST
    status_code: 200
  delegate_to: localhost
